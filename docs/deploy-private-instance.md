# NanoFlow 私有实例部署指南

> 🎯 **目标**：5 分钟内拥有自己的 NanoFlow 实例，数据完全在你的掌控中

---

## 目录

- [准备工作](#准备工作)
- [第一步：创建 Supabase 项目](#第一步创建-supabase-项目约-3-分钟)
- [第二步：创建 Storage 存储桶](#第二步创建-storage-存储桶约-30-秒)
- [第三步：执行数据库初始化脚本](#第三步执行数据库初始化脚本约-1-分钟)
- [第四步：获取 API 密钥](#第四步获取-api-密钥约-30-秒)
- [第五步：部署到 Vercel](#第五步部署到-vercel约-2-分钟)
- [第六步：验证部署成功](#第六步验证部署成功)
- [故障排查指南](#故障排查指南)
- [升级与维护](#升级与维护)
- [附录](#附录)

---

## 准备工作

在开始之前，你需要：

| 必需项 | 说明 | 获取方式 |
|--------|------|----------|
| GitHub 账号 | 用于登录 Vercel 和 Supabase | [注册 GitHub](https://github.com/signup) |
| 浏览器 | 推荐 Chrome 或 Firefox | - |

> 💡 全程无需安装任何软件，所有操作在浏览器中完成

---

## 第一步：创建 Supabase 项目（约 3 分钟）

Supabase 是一个开源的 Firebase 替代品，提供免费的 PostgreSQL 数据库。

### 1.1 注册/登录 Supabase

1. 打开 [supabase.com](https://supabase.com)
2. 点击 **「Start your project」** 或 **「Dashboard」**
3. 选择 **「Continue with GitHub」**（最快）
4. 授权 GitHub 登录

### 1.2 创建新项目

1. 在 Dashboard 页面，点击 **「New project」**
2. 填写项目信息：
   - **Name**：`my-nanoflow`（或任意名称）
   - **Database Password**：设置一个强密码
     - ⚠️ **务必保存此密码**，虽然本应用不直接使用，但可能需要用于数据库管理
   - **Region**：选择离你最近的区域
     - 推荐：`Northeast Asia (Tokyo)` 或 `Singapore`
3. 点击 **「Create new project」**
4. 等待项目初始化（约 1-2 分钟）
   - 看到 **「Project is ready」** 即可进行下一步

---

## 第二步：创建 Storage 存储桶（约 30 秒）

Storage 用于存储任务附件（图片、文件等）。

1. 在左侧菜单找到 **「Storage」**，点击进入
2. 点击 **「New bucket」** 按钮
3. 填写存储桶信息：
   - **Name**：`attachments`（⚠️ 必须是这个名称）
   - **Public bucket**：❌ **不要勾选**（保持私有）
4. 点击 **「Create bucket」**

✅ 完成后，你会在存储桶列表中看到 `attachments`

---

## 第三步：执行数据库初始化脚本（约 1 分钟）

这一步会创建应用所需的所有数据库表、函数和安全策略。

### 3.1 打开 SQL Editor

1. 在左侧菜单找到 **「SQL Editor」**，点击进入
2. 点击 **「+ New query」** 创建新查询

### 3.2 获取初始化脚本

1. 打开本仓库的 [`scripts/init-supabase.sql`](../scripts/init-supabase.sql) 文件
2. 点击 **「Raw」** 按钮查看原始文件
3. 使用 `Ctrl+A`（Mac: `Cmd+A`）全选
4. 使用 `Ctrl+C`（Mac: `Cmd+C`）复制

### 3.3 执行脚本

1. 回到 Supabase SQL Editor
2. 使用 `Ctrl+V`（Mac: `Cmd+V`）粘贴脚本
3. 点击 **「Run」** 按钮（或按 `Ctrl+Enter`）
4. 等待执行完成（约 5-15 秒）
5. 看到 ✅ **Success** 表示成功

### 3.4 验证表创建成功

1. 点击左侧 **「Table Editor」**
2. 应该能看到以下表：
   - `projects`
   - `tasks`
   - `connections`
   - `user_preferences`

> ⚠️ **常见问题**：如果提示 "syntax error"，检查是否完整复制了脚本（不要遗漏开头或结尾）

---

## 第四步：获取 API 密钥（约 30 秒）

### 4.1 进入 API 设置

1. 在左侧菜单最下方，点击 **「Project Settings」** ⚙️
2. 点击 **「API」**（在 Configuration 分组下）

### 4.2 复制连接信息

你需要复制以下两个值（建议先保存到记事本）：

**Project URL**
```
https://xxxxxxxx.supabase.co
```
点击旁边的 📋 复制按钮

**Project API keys - anon public**
```
eyJhbGciOiJIUzI1NiIs...（很长的一串字符）
```
点击 `anon public` 旁边的 📋 复制按钮

> ⚠️ **安全警告**：
> - ✅ 使用 `anon public` 密钥
> - ❌ **不要**使用 `service_role` 密钥！它有完全权限，泄露会导致数据被删除

---

## 第五步：部署到 Vercel（约 2 分钟）

### 5.1 点击部署按钮

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2Fdydyde%2Fdde&env=NG_APP_SUPABASE_URL,NG_APP_SUPABASE_ANON_KEY&envDescription=Supabase%20%E9%85%8D%E7%BD%AE%EF%BC%88%E4%BB%8E%20Supabase%20Dashboard%20%3E%20Settings%20%3E%20API%20%E8%8E%B7%E5%8F%96%EF%BC%89&envLink=https%3A%2F%2Fgithub.com%2Fdydyde%2Fdde%23supabase-%E9%83%A8%E7%BD%B2%E9%85%8D%E7%BD%AE&project-name=my-nanoflow&repository-name=my-nanoflow)

### 5.2 登录 Vercel

如果没有 Vercel 账号：
1. 选择 **「Continue with GitHub」** 用 GitHub 登录
2. 授权 Vercel 访问你的 GitHub

### 5.3 配置项目

在 **「Configure Project」** 页面：

1. **Repository Name**：`my-nanoflow`
   - 这会 Fork 仓库到你的 GitHub 账号

2. **Environment Variables**（重要！）：
   
   | 变量名 | 值 |
   |--------|-----|
   | `NG_APP_SUPABASE_URL` | 粘贴你的 Project URL<br>`https://xxxxxxxx.supabase.co` |
   | `NG_APP_SUPABASE_ANON_KEY` | 粘贴你的 anon public key<br>`eyJhbGciOiJIUzI1NiIs...` |

3. 点击 **「Deploy」** 按钮

### 5.4 等待部署完成

- 构建过程约 2-3 分钟
- 看到 **「Congratulations!」** 表示部署成功 🎉
- 点击 **「Continue to Dashboard」** 或访问显示的 URL

---

## 第六步：验证部署成功

使用以下清单验证你的私有实例是否正常工作：

| ✓ | 检查项 | 预期结果 |
|---|--------|----------|
| □ | 访问你的 Vercel URL | 能正常打开页面 |
| □ | 看到登录/注册页面 | 显示登录表单 |
| □ | 注册新账号 | 收到验证邮件（或直接登录成功） |
| □ | 创建新项目 | 项目保存成功 |
| □ | 刷新页面 | 数据仍然存在 |
| □ | 上传附件 | 文件上传成功 |

**全部 ✓？恭喜，你的私有 NanoFlow 已完全就绪！** 🎉

---

## 故障排查指南

### 错误 1：构建失败 - 环境变量未设置

```
Error: Supabase 环境变量未配置
```

**原因**：Vercel 环境变量未正确设置

**解决**：
1. 进入 Vercel Dashboard → 你的项目 → Settings → Environment Variables
2. 确认 `NG_APP_SUPABASE_URL` 和 `NG_APP_SUPABASE_ANON_KEY` 已设置
3. 检查值是否完整（没有多余空格）
4. 重新部署：Deployments → 最新部署 → 三点菜单 → Redeploy

### 错误 2：登录失败 - 401 Unauthorized

```
Error: Invalid API key
```

**原因**：
- 使用了错误的 Supabase Key
- Supabase 项目未完成初始化

**解决**：
1. 确认使用的是 `anon public` key（不是 `service_role`）
2. 检查 Key 是否完整复制（无多余空格或缺失字符）
3. 等待 Supabase 项目初始化完成（约 2 分钟）

### 错误 3：数据库操作失败 - 42P01 relation does not exist

```
Error: relation "projects" does not exist
```

**原因**：SQL 初始化脚本未执行

**解决**：
1. 进入 Supabase Dashboard → SQL Editor
2. 重新执行 `init-supabase.sql`
3. 确认执行成功（绿色 Success 提示）

### 错误 4：附件上传失败 - Storage bucket not found

```
Error: Bucket not found
```

**原因**：未创建 Storage bucket

**解决**：
1. 进入 Supabase Dashboard → Storage
2. 点击 "New bucket"
3. 名称填入 `attachments`，取消勾选 "Public bucket"
4. 点击创建

### 错误 5：页面空白或无限加载

**原因**：可能是环境变量格式错误

**解决**：
1. 检查 URL 是否以 `https://` 开头，以 `.supabase.co` 结尾
2. 检查 Key 是否以 `eyJ` 开头（JWT 格式）
3. 打开浏览器开发者工具（F12）→ Console，查看具体错误

---

## 升级与维护

### 版本升级

你的私有实例有三种升级方式：

#### 方式 1：自动同步（推荐）

1. 进入你 Fork 的 GitHub 仓库
2. 点击 **「Sync fork」** 按钮
3. 点击 **「Update branch」**
4. Vercel 会自动检测更新并重新部署

#### 方式 2：手动重新部署

1. 进入 Vercel Dashboard
2. 选择你的项目 → Deployments
3. 点击最新部署的三点菜单 → **「Redeploy」**

#### 方式 3：数据库迁移（如有）

1. 查看 [CHANGELOG](../CHANGELOG.md) 确认是否有数据库变更
2. 如有，到 Supabase SQL Editor 执行对应的迁移 SQL
3. 迁移脚本通常是幂等的，重复执行不会出错

### 备份建议

| 备份方式 | 操作 | 频率建议 |
|----------|------|----------|
| **应用内导出** | 设置 → 数据管理 → 导出数据 | 每周一次 |
| **Supabase 备份** | Dashboard → Database → Backups | 自动（Pro 计划）|
| **本地自动备份** | 设置 → 本地自动备份 | 每 30 分钟 |

### （可选）启用定时清理

如果你想让系统自动清理 30 天前删除的任务：

1. 在 Supabase 控制台：Database → Extensions
2. 搜索 `pg_cron`，点击启用
3. 在 SQL Editor 执行：

```sql
SELECT cron.schedule('cleanup-deleted-tasks', '0 3 * * *', $$SELECT cleanup_old_deleted_tasks()$$);
SELECT cron.schedule('cleanup-deleted-connections', '0 3 * * *', $$SELECT cleanup_old_deleted_connections()$$);
SELECT cron.schedule('cleanup-old-logs', '0 4 * * 0', $$SELECT cleanup_old_logs()$$);
```

---

## 附录

### 环境变量完整列表

| 变量名 | 必填 | 说明 | 默认值 |
|--------|------|------|--------|
| `NG_APP_SUPABASE_URL` | ✅ | Supabase 项目 URL | 无 |
| `NG_APP_SUPABASE_ANON_KEY` | ✅ | Supabase 匿名公钥 | 无 |
| `NG_APP_SENTRY_DSN` | ❌ | Sentry 错误监控 DSN | 空（禁用） |
| `SENTRY_DSN` | ❌ | Sentry 错误监控 DSN（别名，常用于 Vercel） | 空（禁用） |
| `NG_APP_GOJS_LICENSE_KEY` | ❌ | GoJS 许可证（移除水印）| 空（显示水印） |
| `NG_APP_DEMO_MODE` | ❌ | 启用 Demo 模式限制 | `false` |

### 日志查看

| 平台 | 日志位置 |
|------|----------|
| Vercel | Dashboard → Project → Logs |
| Supabase | Dashboard → Logs（可按服务筛选）|
| 浏览器 | 开发者工具（F12）→ Console |

### 其他部署平台

#### Netlify

1. Fork 本仓库到你的 GitHub
2. 登录 [Netlify](https://netlify.com)
3. New site from Git → 选择你 Fork 的仓库
4. 在 Site settings → Environment variables 添加：
   - `NG_APP_SUPABASE_URL`
   - `NG_APP_SUPABASE_ANON_KEY`
5. Trigger deploy

#### Railway

1. 登录 [Railway](https://railway.app)
2. New Project → Deploy from GitHub repo
3. 选择你 Fork 的仓库
4. 在 Variables 中添加环境变量
5. Deploy

---

## 获取帮助

如果遇到问题：

1. 查看上方的 [故障排查指南](#故障排查指南)
2. 搜索 [GitHub Issues](https://github.com/dydyde/dde/issues)
3. 提交新 Issue，附上：
   - 错误截图
   - 浏览器控制台日志
   - 你的操作步骤

---

*最后更新: 2026-01-20*
