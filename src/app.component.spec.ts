import { describe, it, expect } from 'vitest';
import { AppComponent } from './app.component';

describe('AppComponent (BootShell)', () => {
  it('should be defined', () => {
    expect(AppComponent).toBeDefined();
  });
});
