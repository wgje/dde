<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <queries>
        <package android:name="com.android.chrome" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/nanoflow_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/nanoflow_launcher_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.NanoFlow.Twa">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/nanoflow_twa_filepaths" />
        </provider>

        <!--
          2026-04-20 TWA 闪白屏根因修复（方案 C：task 彻底隔离）。
          现象：warm tap（HOME 后再次点击图标）出现一瞬 #F9F8F6 splash → Chrome CCT 黑色空窗的
          "闪白"观感。abd logcat 抓到决定性证据：
            ActivityTaskManager: START ... LAUNCH_SINGLE_TASK ... result code=2   // START_TASK_TO_FRONT
            WindowManager: Try to add startingWindow type = STARTING_WINDOW_TYPE_SPLASH_SCREEN
              resolvedTheme = Theme.NanoFlow.Twa.Launcher
          原因：singleTask 让 LauncherActivity 长期驻留为 task 的 baseActivity；warm 点击图标时
          Android 走 START_TASK_TO_FRONT（result code=2）路径，既不创建新实例也不触发
          onCreate/onNewIntent，系统直接基于 baseActivity 的主题强制加 splash window，所有现有
          splash-hold 补丁（installSplashScreen / super.onCreate(null) / onNewIntent override）
          全部无法介入这条路径。
          修复：
          1. 去掉 android:launchMode="singleTask"，改回 standard（默认）。
          2. 加 android:taskAffinity=""：空 affinity 让该 activity 不属于任何 task，每次 LAUNCHER
             intent 都创建全新 task，旧 task 不会被 LAUNCHER intent 命中并 task-to-front。
          3. 加 android:excludeFromRecents="true"：旧 task 不在 recents 里，避免累积。
          4. 加 android:finishOnTaskLaunch="true"：哪怕系统边界情况仍尝试把旧 task 带回前台，
             LauncherActivity 会立即 finish，不会再作为目标容器加 splash。
          效果：每次点图标都走 cold path（onCreate → installSplashScreen 的 splash-hold → launchTwa →
          Chrome CCT）。Chrome CCT intent 默认在此 task 内启动，LauncherActivity 在 CCT 就位后
          被 ABH 基类 finish，旧 task 随 LauncherActivity finish + noHistory-style 清理被系统回收。
          widget intent 路径（intentForWidget 已带 NEW_TASK | CLEAR_TASK）不受影响：空 affinity 下
          CLEAR_TASK 等价于 NEW_TASK，每次都是全新 task。
        -->
        <activity
            android:name="app.nanoflow.host.NanoflowTwaLauncherActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:finishOnTaskLaunch="true"
            android:taskAffinity=""
            android:theme="@style/Theme.NanoFlow.Twa.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="${nanoflowDefaultUrl}" />
            <meta-data
                android:name="asset_statements"
                android:resource="@string/asset_statements" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/nanoflow_twa_launch_background" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR_DARK"
                android:resource="@color/nanoflow_twa_launch_background" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/nanoflow_twa_launch_background" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR_DARK"
                android:resource="@color/nanoflow_twa_launch_background" />
            <!--
              2026-04-19 白屏修复：移除 SPLASH_IMAGE_DRAWABLE / SPLASH_SCREEN_BACKGROUND_COLOR /
              SPLASH_SCREEN_FADE_OUT_DURATION 三项 meta-data。
              原因：Chrome 147 与 ABH 2.5.0 的 PwaWrapperSplashScreenStrategy 在 FileProvider
              splash bitmap 传递握手阶段卡住（SmartPower 日志：CustomTabsConnectionService 绑定
              3s 后因无 session 创建被 idle 解绑），导致 launchWhenSplashScreenReady 的
              completion runnable 永远不触发，CCT intent 永不发出，Activity 自身窗口背景
              (#F9F8F6) 被用户感知为"闪白屏"。去掉 SPLASH_IMAGE_DRAWABLE 后
              LauncherActivityMetadata.splashImageDrawableId==0 → splashScreenNeeded()==false
              → TwaLauncher 跳过 splash 传输阶段，直接 launchWhenSplashScreenReady，立即发
              TrustedWebActivity intent 给 Chrome。Activity 的 windowBackground/SystemSplash
              仍提供视觉连续性。 -->
            <meta-data
                android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
                android:value="${applicationId}.fileprovider" />
        </activity>

        <activity
            android:name="app.nanoflow.host.NanoflowWidgetBootstrapActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="bootstrap"
                    android:scheme="nanoflow-widget" />
            </intent-filter>
        </activity>

        <activity
            android:name="app.nanoflow.host.NanoflowWidgetActionActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.NanoFlow.WidgetAction" />

        <receiver
            android:name="app.nanoflow.host.NanoflowWidgetReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="@string/nanoflow_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
                        <!--
                            2026-04-21 小组件实时性修复：FCM 未就绪时，USER_PRESENT 是 manifest receiver
                            仍稳定可用的「用户即将看 widget」信号。这里走 2 min 节流（解锁=明确要看）；
                            Click 系列 action 会重置节流闸，让「点 widget 进应用 → 切专注模式 → 回桌面」
                            闭环走 0 延迟刷新。
                        -->
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>

            <!--
                2026-04-22 根因修复：APK 升级（含 widget layout 结构变化）后，launcher 只会调用
                RemoteViews.reapply（经由 partiallyUpdateAppWidget），不会重新 inflate hostView。
                因此旧 view 树里不存在的新增 View ID（如 nano_widget_footer_label）永远不可见。
                监听 MY_PACKAGE_REPLACED 在升级瞬间对所有 widgetId 走非 partial 的 updateAppWidget，
                强制 hostView 用最新 layout 重新 inflate，破冰一次即可。
            -->
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nanoflow_focus_widget_info" />
        </receiver>

        <service
            android:name="app.nanoflow.host.NanoflowFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
          widget 集合视图 (nano_widget_action_list) 的 RemoteViewsFactory 宿主服务。
          必须声明 android.permission.BIND_REMOTEVIEWS，否则系统 AppWidgetHost 会拒绝绑定。
        -->
        <service
            android:name="app.nanoflow.host.NanoflowWidgetActionService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

    </application>

</manifest>
